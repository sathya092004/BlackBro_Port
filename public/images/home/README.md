Homepage editorial images (Gender Split, Hero, Campaign, etc.) go here.

Gender Split section expects exactly these two filenames:
  gender-split-men.jpg
  gender-split-women.jpg

(Edit the `image` field in src/lib/data/homepage.ts under `genderSplit`
if you want different filenames.)
